//Sliders
const slider = new Swiper(".slider", {
    loop: true,
    pagination: {
        el: '.swiper-pagination',
        clickable: true,
      },
});

const overviewSlider = new Swiper(".overview-slider", {
    loop: true,
    pagination: {
        el: '.swiper-pagination',
        clickable: true,
      },
});